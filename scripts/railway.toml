[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.data-explorer"
buildContext = "."

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHONUNBUFFERED = "1"
