[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.simulation-service"
buildContext = "."

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHONUNBUFFERED = "1"
