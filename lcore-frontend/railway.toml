[build]
builder = "DOCKERFILE"
dockerfilePath = "lcore-frontend/Dockerfile"
buildContext = "lcore-frontend"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
