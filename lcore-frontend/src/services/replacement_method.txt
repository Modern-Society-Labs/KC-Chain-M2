  async getRecentActivity(): Promise<RecentActivity[]> {
    try {
      console.log('🔍 Fetching real-time blockchain activity...');
      
      // Skip broken Blockscout API calls due to CORS issues
      // Go directly to our working cast-based fallback for real-time data
      console.log('🔄 Using cast-based real-time blockchain data (Blockscout API has CORS issues)...');
      const castData = await this.getBlockchainDataViaCast();
      if (castData.length > 0) {
        console.log('✅ Using real blockchain data from cast fallback');
        return castData;
      }
      
      // Final fallback if somehow cast method fails
      console.log('🔄 Providing IoT-focused sample data (InputBox style)');
      return this.getHistoricalSampleData();

    } catch (error) {
      console.error('💥 Error fetching blockchain activity:', error);
      
      // Final fallback: IoT-focused sample data that represents real InputBox usage
      console.log('🔄 Providing IoT-focused sample data (InputBox style)');
      return this.getHistoricalSampleData();
    }
  }
